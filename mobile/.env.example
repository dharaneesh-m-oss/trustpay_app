# Leave blank on a phone: the app derives the API host from the address
# Metro is already serving from, so it survives your IP changing.
# Set it only to point at a different machine or a deployed backend,
# e.g. EXPO_PUBLIC_API_URL=https://api.example.com/api/v1
EXPO_PUBLIC_API_URL=
